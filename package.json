{
  "name": "todo-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready full-stack todo app (React + Express + MongoDB)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "node scripts/ensure-env.cjs",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
