# Taskflow — environment pointers
#
# After `npm install`, `server/.env` is created if missing.
# For local dev you only need: `npm run dev` (in-memory Mongo + dev JWT when unset).
#
# See README.md and server/.env.example.
